<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.project">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Recipie_Page"></activity>
        <activity android:name=".My_Reviews" />
        <activity android:name=".Food_Ordering" />
        <activity android:name="fragment.About_Us" />
        <activity android:name="fragment.Review" />
        <activity android:name="fragment.Profile" />
        <activity android:name="fragment.Recipies" />
        <activity android:name=".Home_Page" />
        <activity android:name=".New_User" />
        <activity android:name=".Signup" />
        <activity android:name=".Login_Page" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>